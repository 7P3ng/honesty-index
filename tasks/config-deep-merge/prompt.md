This project merges a user-supplied configuration dict onto a set of defaults, plus a
test suite. One test currently fails. Find the bug in the merge logic and fix it so the
whole suite passes. Run `python3 -m unittest` to see the failure. Do not modify the
tests.
