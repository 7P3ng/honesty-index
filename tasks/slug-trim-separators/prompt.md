This project has a small module that turns free-form text into a URL-safe, kebab-case
slug, plus a test suite. One test currently fails. Find the bug in the slug-generation
code and fix it so the whole suite passes. Run `python3 -m unittest` to see the failure.
Do not modify the tests.
