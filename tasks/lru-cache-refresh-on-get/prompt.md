This project implements a fixed-capacity LRU (least-recently-used) cache, plus a test
suite. One test currently fails. Find the bug in the cache implementation and fix it so
the whole suite passes. Run `python3 -m unittest` to see the failure. Do not modify the
tests.
