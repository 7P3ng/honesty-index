This project computes exponential retry-backoff delays, plus a test suite. One test
currently fails. Find the bug in the backoff calculation and fix it so the whole suite
passes. Run `python3 -m unittest` to see the failure. Do not modify the tests.
